export class User {
    constructor(email, firstName, lastName, dateOfBirth, password) {
        this.email = email;
        this.firstName = firstName;
        this.lastName = lastName;
        this.dateOfBirth = dateOfBirth;
        this.password = password;
    }
}
